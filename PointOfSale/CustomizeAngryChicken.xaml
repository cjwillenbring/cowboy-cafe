<UserControl x:Class="PointOfSale.CustomizeAngryChicken"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Height="200" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="30" Text="Angry Chicken Customization" Grid.Row="0"></TextBlock>
        <ToggleButton IsChecked="{Binding Path=Bread}" Grid.Row="1" Click="OnEventChange">Bread</ToggleButton>
        <ToggleButton IsChecked="{Binding Path=Pickle}" Grid.Row="2" Click="OnEventChange">Pickle</ToggleButton>
    </Grid>
</UserControl>
